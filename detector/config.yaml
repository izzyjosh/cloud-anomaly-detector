log:
  path: /var/log/hng-access.log 

slack:
  webhook_url: ${WEB_HOOK_URL}

thresholds:
  z_score: 3.0
  spike_multiplier: 2.0
  error_multiplier: 1.5
  window_size: 60

blocker:
  ban_duration:
    - 600
    - 1800
    - 7200
    - -1