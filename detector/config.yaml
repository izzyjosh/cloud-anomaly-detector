log:
  path: /var/log/hng-access.log 

slack:
  webhook_url: ${WEB_HOOK_URL}

thresholds:
  z_score: 3.0
  spike_multiplier: 5.0
  error_multiplier: 3.0
  window_size: 60

blocker:
  ban_duration:
    - 600
    - 1800
    - 7200
    - -1